// Configuração padrão do Metro do Expo.
// Estende "expo/metro-config" (recomendado pelo expo-doctor).
const { getDefaultConfig } = require('expo/metro-config');

const config = getDefaultConfig(__dirname);

module.exports = config;
